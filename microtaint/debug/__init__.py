"""Debug and test-only helpers.

Nothing in this package is imported by the taint compute path (the sleigh
engine or the cell kernels).  These modules exist so humans can write, read and
inspect tests and rules in architectural terms while the engine keeps its
ISA-general internal names.
"""
