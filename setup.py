from __future__ import annotations

from setuptools import find_packages, setup

setup(
    name='pcode_taint',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Add required dependencies like pypcode, etc.
    ],
)
